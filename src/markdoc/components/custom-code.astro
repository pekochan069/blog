---
import { Code } from "astro-expressive-code/components";
import MathComponent from "./math.astro";

const { code, lang } = Astro.props;
---

{lang !== "math" && <Code code={code} lang={lang} wrap={false} />}
{lang === "math" && <MathComponent src={code} />}
