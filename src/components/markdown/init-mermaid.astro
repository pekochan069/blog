<script>
  import mermaid from "mermaid";

  (() => {
    // @ts-ignore
    if (window.mermaid) {
      return;
    }

    // @ts-ignore
    window.mermaid = mermaid;
    // @ts-ignore
    window.mermaid.initialize({
      theme: "default",
    });
  })();
</script>
